# dimenso — pinned versions for REFERENCE ONLY.
#
# DO NOT `pip install -r` this into a fresh env to get Isaac Sim / Isaac Lab / torch.
# Those live in the existing `isaac_sim` conda env (Isaac Sim 5.1 + Isaac Lab 0.54.3 +
# torch 2.12 nightly cu128 for the RTX 5060 Blackwell GPU) and must NOT be reinstalled
# or cloned. See CLAUDE.md. This file documents what dimenso depends on and the three
# perception packages that were added directly to `isaac_sim`.

# --- already in isaac_sim (do NOT reinstall) ---
# isaacsim==5.1.0.0
# isaaclab==0.54.3            # editable, from ~/projects/IsaacLab
# torch==2.12.0.dev20260306+cu128
# numpy==1.26.0
# warp-lang==1.12.0

# --- added directly to isaac_sim for dimenso perception (P2) ---
mediapipe==0.10.21
mujoco==3.9.0
opencv-python==4.11.0.86
# note: installing mediapipe downgraded protobuf 7.34.0 -> 4.25.9 (onnx still happy,
# requires protobuf>=4.25.1). Rollback snapshot: ~/dimenso_isaac_sim_freeze_backup.txt

# --- P4 control panel (add when implementing the server) ---
# fastapi
# uvicorn
