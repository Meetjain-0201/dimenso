"""Simulation layer: the dimenso-owned G1 apple-in-basket Isaac Lab scene.

Importing the scene (`from dimenso.sim import scene`) requires a launched Isaac Sim
app (it pulls `pxr` via `isaaclab.sim`). Do not import it at package import time —
`run_headless.py` launches the app first, then imports it to register the task
`Dimenso-AppleBasket-G1-v0`.
"""
